# ─────────────────────────────────────────────────────────────────────────────
# job_finder_v2 — Python dependencies
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.5.0

# LLM providers
anthropic>=0.39.0
openai>=1.50.0                  # fallback

# Browser automation (universal form filler)
stagehand>=3.0.0
playwright>=1.48.0

# Job discovery (scrapers)
httpx>=0.27.0                   # async HTTP client for JSearch + Greenhouse APIs
beautifulsoup4>=4.12.0          # Lever HTML scraping
lxml>=5.3.0                     # faster BS4 parser

# Resume parsing & document generation
pypdf>=4.0.0                    # extract text from uploaded PDF resumes
python-docx>=1.1.0              # extract text from uploaded DOCX resumes
reportlab>=4.2.0                # render markdown → PDF (pure Python, no system deps)

# Database & encryption
cryptography>=43.0.0            # Fernet encryption for PII fields
python-dotenv>=1.0.0            # .env loading (also handled by pydantic-settings)

# Model Context Protocol
mcp>=1.0.0

# Email tracking (Outlook IMAP)
imapclient>=3.0.0               # clean IMAP client
email-validator>=2.2.0          # validate email addresses

# Utilities
python-dateutil>=2.9.0          # flexible date parsing
fuzzywuzzy>=0.18.0              # company name matching for email → application linking
python-Levenshtein>=0.25.0      # speedup for fuzzywuzzy
uuid>=1.30                      # UUIDs

# Dev / Test
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
httpx>=0.27.0                   # also used for async test client
mypy>=1.11.0
ruff>=0.6.0
